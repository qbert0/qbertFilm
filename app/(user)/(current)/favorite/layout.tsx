
interface Props {
    children : React.ReactNode;
}

const FavoriteLayout = ({children}: Props) => {

    return (
        <>   
          {children}
            
        </>
    )
}

export default FavoriteLayout;